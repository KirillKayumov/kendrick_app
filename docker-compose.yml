version: '3'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    command: ./scripts/dev/setup.sh
    tty: true
    ports:
      - "4000:4000"
    depends_on:
      - postgres
      - frontend

  frontend:
    build:
      context: ./assets
      dockerfile: Dockerfile.dev
    tty: true
    ports:
      - "3000:4000"

  postgres:
    image: postgres:12.0
    environment:
      POSTGRES_DB: kendrick_dev
